package com.example.nhentai.ui.theme

import androidx.compose.ui.graphics.Color

// ── 主色（玫红）──
val RosePrimary = Color(0xFFE91E8C)
val RosePrimaryDark = Color(0xFFFF4DB8)
val RosePrimaryDim = Color(0xFFB5146B)

// ── 深色模式色板 ──
val DarkBackground = Color(0xFF0D0D0F)
val DarkSurface = Color(0xFF1A1A1E)
val DarkSurfaceVariant = Color(0xFF252530)
val DarkOutline = Color(0xFF3A3A45)

// ── 内容色 ──
val OnDarkPrimary = Color(0xFFFFFFFF)
val OnDarkBackground = Color(0xFFE8E8EF)
val OnDarkSurface = Color(0xFFCCCCD6)
val OnDarkSurfaceVariant = Color(0xFF9999AA)

// ── 亮色模式色板 ──
val LightBackground = Color(0xFFFAF8FF)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFF0EEF8)
val LightOutline = Color(0xFFD0CEDB)

// ── 功能色 ──
val ErrorColor = Color(0xFFCF6679)
val ErrorDark = Color(0xFFFF8FA3)
val SuccessColor = Color(0xFF4CAF81)