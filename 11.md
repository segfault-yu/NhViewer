NhViewer 屏幕适配与响应式 UI 优化实施计划
现状问题分析 (基于提供的截图与代码)
从您提供的截图和当前代码库中，我们识别出以下几个核心问题：

Edge-to-Edge (全面屏) 边距处理异常 (如截图红框所示)：
截图 1 (首页)：顶部状态栏区域出现大片空白，内容被过度向下推挤（Double Padding 现象）。这是因为 MainActivity 中的 Scaffold 和 HomeScreen 中的 Scaffold 嵌套，导致系统状态栏高度被计算了两次。
截图 2 (详情页)：顶部的 TopAppBar 文本与系统状态栏（时间、信号）完全重叠；底部的标签列表/内容与系统的手势导航条（小白条）重叠。这是由于在隐藏了底栏的场景下，Insets 没有被正确消耗和传递给子屏幕的 Scaffold。
缺乏平板/大屏响应式适配：
所有的画廊列表（首页、搜索、标签等）都硬编码了 StaggeredGridCells.Fixed(2) 或 GridCells.Fixed(2)。这在手机上没问题，但在平板或横屏下，封面会被拉伸得非常巨大，极度影响阅读体验。
阶段 1：修复 Edge-to-Edge 边距与 Scaffold 嵌套结构
目标：重构全局的 Scaffold 嵌套，确保状态栏和底部导航栏完美融入界面，无重叠、无白边。

任务清单
1.1 [MODIFY] 
MainActivity.kt
问题：NavigationSuiteScaffold 内部包裹了一个 Scaffold，然后又传递给了 NhViewerNavGraph。
修改：移除内部冗余的 Scaffold。让 NavigationSuiteScaffold 直接包裹 NhViewerNavGraph。M3 的子页面自带 Scaffold，会自行处理 WindowInsets。
1.2 [MODIFY] 
HomeScreen.kt
 (及其他带底栏的页面)
问题：HomeScreen 的 Scaffold 未正确消耗 Insets，导致顶部双重边距。
修改：确保 Column(modifier = Modifier.padding(innerPadding)) 正确应用。由于 MainActivity 移除了多余的 Scaffold，这里的 innerPadding 将能正确拿到安全的区域。
1.3 [MODIFY] 
DetailScreen.kt
问题：底部内容与手势条重叠（截图2）。
修改：在 DetailScreen 的 Scaffold 内容区 Box 中，确保传入 modifier = Modifier.padding(innerPadding)。检查 LazyVerticalStaggeredGrid 的 contentPadding 是否正确叠加了底部的 safeDrawingPadding，确保输入框不被小白条遮挡。
1.4 [MODIFY] 
ReaderScreen.kt
问题：阅读器的浮动工具栏可能与状态栏冲突。
修改：在 ReaderTopBar 和 ReaderBottomBar 上分别添加 .windowInsetsPadding(WindowInsets.safeDrawing.only(WindowInsetsSides.Top)) 和 Bottom，确保工具栏始终避开刘海屏和手势条。
🔍 检查点 (Checkpoint P1)
检查项	通过标准
首页顶部边距	首页的 "最新/热门" Tab 应当紧贴在状态栏下方，不再有巨大的白色空隙。
详情页顶部	TopAppBar 的后退按钮和标题不再与系统时间、电量等图标重叠。
详情页底部	滚动到底部时，内容或评论输入框位于手势导航小白条的上方。
🐛 Bug 复核点 (Bug Review P1)
 切换到 SearchScreen 激活搜索框时，键盘弹起是否会遮挡搜索历史列表？（需验证 IME Insets）。
 隐藏系统状态栏全屏阅读时，再次呼出状态栏是否会导致画面突然跳动？
阶段 2：全局响应式网格布局 (Tablet & Foldable Ready)
目标：抛弃硬编码的 2 列布局，改用自适应宽度，让平板和折叠屏自动展示 3-6 列画廊。

任务清单
2.1 [MODIFY] 
HomeScreen.kt
将 StaggeredGridCells.Fixed(2) 修改为 StaggeredGridCells.Adaptive(minSize = 160.dp)。
将热门 Tab 的 GridCells.Fixed(2) 修改为 GridCells.Adaptive(minSize = 160.dp)。
2.2 [MODIFY] 
SearchScreen.kt
将搜索结果的 StaggeredGridCells.Fixed(2) 修改为 StaggeredGridCells.Adaptive(160.dp)。
2.3 [MODIFY] 
TagsScreen.kt
标签列表属于纯文本块，2列在大屏下也会太宽。将 GridCells.Fixed(2) 改为 GridCells.Adaptive(minSize = 180.dp)。
2.4 [MODIFY] 
DetailScreen.kt
相关推荐列表：修改为 Adaptive(160.dp)。
2.5 其他列表页面
同步修改 FavoritesScreen、TaggedGalleriesScreen、BlacklistScreen 中的网格布局。
🔍 检查点 (Checkpoint P2)
检查项	通过标准
手机竖屏显示	依然保持一行 2 列的经典显示效果。
手机横屏/平板显示	自动展开为 3-5 列，封面图片不再被过度拉伸放大。
🐛 Bug 复核点 (Bug Review P2)
 改为 Adaptive 后，首页的 HistorySection（最近阅读横向滑动模块）跨列属性 span = StaggeredGridItemSpan.FullLine 是否仍然正常工作？
 在平板上，网格列数增多后，分页加载 (Paging 3) 的触发时机是否仍然灵敏？
阶段 3：详情页结构与阅读器大屏优化
目标：解决大尺寸屏幕下，详情页头部信息过度拉伸，以及阅读器比例适配问题。

任务清单
3.1 [MODIFY] 1
DetailScreen.kt
问题：详情页顶部的封面和文字介绍使用了 weight(0.4f) 和 weight(0.6f)，在横屏平板上会被拉伸得很扁。
修改：将该 Row 限制最大宽度，或者使用更灵活的尺寸修饰符（如封面图 width 限制在 140.dp - 240.dp 之间，右侧文字区域自动填充），避免被无限拉宽。
3.2 [MODIFY] 
ReaderScreen.kt
确保 ZoomableBox 中的图片在平板横屏时，ContentScale.Fit 能够正确居中，避免图片靠边。
检查 Pager 模式在大屏幕下的手势防呆区域。
🔍 检查点 (Checkpoint P3)
检查项	通过标准
平板详情页	详情页顶部的封面图片尺寸合理，文字不会分散在过宽的屏幕两端。
📅 实施时间线预估
阶段 1 (Edge-to-Edge 修复)：半天
阶段 2 (响应式网格改造)：半天
阶段 3 (细节优化)：半天