NhViewer 登录/注册/Token/会话管理链路重构实施计划
现状问题分析 (基于现有代码库)
经过对当前代码库的深度分析，目前的认证链路不仅存在稳定性隐患，在交互体验上也较为臃肿，主要存在以下问题：

🐌 登录流程串行，等待时间过长 (Slow Login Flow)： 当前用户点击“登录”后，程序会串行执行：请求 PoW 配置 -> 算力解算 PoW -> 请求 Captcha 配置 -> 呼出验证码。由于中间包含了两次网络请求和一次 CPU 密集计算，用户点击登录后会有明显的迟滞感。
🚨 网络请求死循环与死锁风险 (Circular Dependency)： AuthApi 使用了全局的、挂载了 AuthInterceptor 和 TokenRefreshAuthenticator 的 OkHttpClient。当刷新 Token 时遇到 401，会导致无限循环请求或 OkHttp 线程池死锁。
🚨 状态断层与静默失败 (Silent Failure)： 如果 Token 彻底失效，底层 TokenManager 清除后，UI 层若不处在首页，并不会自动弹回登录页，而是发生页面报错。
阶段 1：登录流程极致加速 (Fast Login Optimization)
目标：消除点击登录后的卡顿感，实现“秒开人机验证”，极大地简化并加速登录体验。

任务清单
[MODIFY] AuthViewModel.kt：
引入后台预加载 (Prefetching)：添加 prefetchSecurityConfigs() 方法。在用户打开 AuthScreen 的一瞬间，就静默在后台并发（async）请求 PoW 挑战和 Captcha 配置。
预解算 PoW：PoW 挑战拿到后，立即在后台使用 PowSolver 算出 Nonce 并缓存。
拦截与等待机制：当用户点击“登录”时，如果预加载已完成，立刻展示验证码弹窗；如果还在加载中，则等待其完成。
清理繁琐的 UI 状态：移除当前烦人的 powStatus 强制弹窗步骤展示，改为进度条或在后台静默处理，不再打断用户的操作心流。
🔍 检查点 (Checkpoint P1)
用户输入完账号密码点击“登录”后，能几乎瞬间（取决于网络）弹出人机验证码，而不是经历漫长的四个文本状态切换。
阶段 2：网络层解耦 (Network Layer Decoupling)
目标：彻底分离“需要鉴权的请求”和“不需要鉴权的请求”（如登录、注册、刷新 Token），打破循环依赖，确保请求稳定。

任务清单
[MODIFY] di/NetworkModule.kt：
拆分 OkHttpClient 为两份：
BaseOkHttpClient：不挂载 AuthInterceptor 和 Authenticator。
AuthOkHttpClient：通过 .newBuilder() 继承 Base，加上鉴权拦截器。
拆分 Retrofit 实例：
BaseRetrofit：专用于提供 AuthApi。
AuthRetrofit：用于提供 GalleryApi, FavoriteApi 等业务接口。
[MODIFY] TokenRefreshAuthenticator.kt：移除针对 Lazy<AuthApi> 可能引发的循环注入问题，确保刷新 Token 请求不带过期的 Header。
🔍 检查点 (Checkpoint P2)
登录、注册、密码重置等功能正常发起。
抓包检查 Token 刷新请求，确保其 Header 中不再携带旧的、会导致 401 报错的 Authorization: Bearer <expired>。
阶段 3：Token 刷新并发控制与持久化加固
目标：处理并发请求引发的“雷群效应”，确保即使同时发起 10 个失败的请求，也只会静默触发 1 次刷新 Token 调用。

任务清单
[MODIFY] TokenRefreshAuthenticator.kt：
在 Mutex.withLock 内实施严密的双重校验锁 (Double-checked locking)：进入锁后，再次对比 Request Header 中的 Token 与本地最新 Token。如果不一致，说明已经被其他协程刷新成功，直接重试即可，避免重复请求。
[MODIFY] TokenManager.kt：
引入全局 SharedFlow<AuthEvent>。当刷新 Token 彻底失败（例如 Refresh Token 也过期了），除了清理本地缓存外，还要发射 AuthEvent.SessionExpired 事件。
阶段 4：状态管理与全局路由强制响应
目标：确保底层的 Token 过期事件能够优雅地反映到全局 UI，将用户踢回登录界面，并在完成上述所有步骤后，确保一切正常运作。

任务清单
[MODIFY] UserRepositoryImpl.kt：统一管理认证状态，监听并转发 SessionExpired 事件。
[MODIFY] MainActivity.kt 或根 NavGraph：
订阅认证状态事件。如果捕获到 SessionExpired 且用户当前处于需要登录的页面（如收藏夹），弹出 Snackbar 提示：“您的登录信息已过期，请重新登录”。
自动执行 navController.navigate(Route.Auth) 并清空返回栈 (popUpTo(0))，防止按返回键进入失效页面。
接入已有的会话管理 API，为未来设置页面的“会话与设备管理”打通通道。
🐛 Bug 复核点 (Bug Review P4)
 预加载 PoW 时，如果用户故意输错了密码导致登录失败，预加载的 PoW Nonce 被消耗后，再次点击登录是否能正确申请新的 PoW 挑战？
 强制退出时，正在进行的业务网络请求是否能被正确中止并清理内存泄露？