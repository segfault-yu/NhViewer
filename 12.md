NhViewer 综合设置中心实施计划 (基于 API 能力扩展)
发散性思考：我们能提供哪些设置？
通过阅读 nhentai_api_v2.txt，结合现有代码和常规漫画阅读器功能，我们可以在侧边栏添加一个全新的**“设置 (Settings)”**页面，包含以下几大板块：

账户与安全 (Account & Security)
设备与会话管理 (/api/v2/auth/sessions)：展示当前用户登录的所有设备，支持远程“踢下线”。
API 密钥管理 (/api/v2/user/keys)：高级功能，允许用户申请和吊销自己的 API Token。
下载与存储 (Download & Storage)
默认下载格式：配合 API 的下载端点 (/api/v2/galleries/{id}/download)，让用户选择默认是打包成 ZIP 还是 CBZ。
缓存清理：一键清理本地 Coil 图片缓存。
阅读器偏好 (Reader Preferences)
默认阅读方向：从右向左 (日漫)、从左向右 (美漫)、上下滚动 (条漫)。
屏幕常亮：阅读页面强制保持屏幕常亮。
外观与显示 (Appearance)
深色模式与动态取色：跟随系统/始终开启/始终关闭，以及 Android 12+ 的 Material You 颜色开关。
网格密度 (自适应宽度)：提供一个滑块（Slider），允许用户在 120dp 到 240dp 之间调节，从而自由决定平板或手机上一行显示几列画廊。
内容过滤 (Content Filters)
黑名单管理 (/api/v2/blacklist)：将原本暴露在侧边栏外面的黑名单移入设置中。
阶段 1：侧边栏入口与本地存储基建
目标：打通导航，搭建 DataStore 存储架构。

任务清单1
[MODIFY] presentation/navigation/Route.kt：新增 data object Settings : Route。
[MODIFY] presentation/navigation/AppDrawerContent.kt：在抽屉列表底部加入带有 Icons.Default.Settings 的“设置”菜单项。
[NEW] data/local/SettingsManager.kt：利用 DataStore<Preferences> 创建用户配置管理器，对外暴露诸如 themeMode, gridBaseWidth, readerDirection 等 Flow 状态流。
[NEW] presentation/feature/settings/SettingsScreen.kt：搭建一个基于 Scaffold 的设置主页面，利用 M3 的 ListItem 将设置项分门别类显示。
🔍 检查点 (Checkpoint P1)
侧边栏可以成功点击进入空的设置页面，且能够正常返回。
依赖注入 (Hilt) 正确绑定了 SettingsManager。
阶段 2：账户与网络高级设置 (API 深度集成)
目标：将 API 文档中强大的安全管理功能落地到 App 中。

任务清单
[NEW] presentation/feature/settings/sessions/SessionScreen.kt：
调用 GET /api/v2/auth/sessions 渲染设备列表。
为每个设备增加 DELETE 按钮以强制吊销。
[NEW] presentation/feature/settings/apikeys/ApiKeyScreen.kt：
实现申请密钥的页面，注意处理 API 要求的 PoW (工作量证明) 和 CAPTCHA。
[MODIFY] SettingsScreen.kt：增加“默认下载格式”弹窗选择，结果保存至 SettingsManager，并在触发下载接口时带入该参数 (?format=zip/cbz)。
🐛 Bug 复核点 (Bug Review P2)
 当前设备自身的 Session 被“踢下线”时，APP 是否能正确捕获 401 Unauthorized 并在拦截器中触发本地 TokenManager.clear() 并返回登录页？
 API Key 的一次性明文展示是否做到了阅后即焚，防止由于界面重建 (Recomposition) 导致意外丢失？
阶段 3：阅读器与 UI 个性化设置
目标：把 UI 控制权交给用户。

任务清单
外观设置：
在 SettingsScreen 增加“主题模式”和“动态取色”的 Switch 与 Dropdown 选项。
修改 MainActivity.kt / NhViewerTheme，使其读取 SettingsManager.themeMode 流，从而实时切换 App 颜色。
布局设置：
增加“网格基准宽度” Slider (滑块组件)。
修改 HomeScreen、SearchScreen 等列表页面，将上一个实施计划中的 Adaptive(160.dp) 绑定到 SettingsManager.gridBaseWidth.collectAsState()，实现滑动滑块，列数实时变动。
阅读器设置：
增加“默认阅读方向”选项。修改 ReaderScreen.kt 中的 Pager 行为，将默认起始位置和滑动方向与该设置对应。
🔍 检查点 (Checkpoint P3)
拖动网格基准宽度滑块时，不用重启 APP 就能看到后面的列表列数发生变化。
切换深色模式立即生效。
阶段 4：黑名单重组与清理功能
目标：使侧边栏更加整洁，完善 APP 收尾体验。

任务清单
[MODIFY] AppDrawerContent.kt：移除现有的“黑名单设置”菜单项。
[MODIFY] SettingsScreen.kt：在“内容过滤”分组下，将黑名单作为一个点击条目 (ListItem)，点击后跳转至已有的 Route.Blacklist 页面。
[MODIFY] SettingsScreen.kt：利用 LocalContext.current.imageLoader.diskCache?.clear() 实现“清理图片缓存”功能，并动态计算展示当前缓存占用的大小 (如：15.2 MB)。
🐛 Bug 复核点 (Bug Review P4)
 移除主侧边栏的黑名单入口后，确保之前的 Navigation Backstack 逻辑不会受到破坏，设置页进入黑名单后点击“返回”应回到设置页。
 清理缓存操作需要在子线程 (Dispatchers.IO) 运行，以免阻塞主线程导致卡顿甚至 ANR。